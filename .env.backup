# --- Sigma Labs Website Production Environment Variables ---

# PostgreSQL connection string (replace with your actual credentials)
DATABASE_URL="postgresql://<username>:<password>@<host>:<port>/<database>"

# NextAuth configuration
NEXTAUTH_URL="https://your-production-domain.com"  # Replace with your deployed domain
NEXTAUTH_SECRET="your-random-secret-key"           # Generate a strong random string

# Solana RPC endpoint (mainnet)
NEXT_PUBLIC_SOLANA_RPC_URL="https://api.mainnet-beta.solana.com"

# --- End of .env ---

# INSTRUCTIONS:
# 1. Replace <username>, <password>, <host>, <port>, and <database> with your PostgreSQL details.
# 2. Set NEXTAUTH_URL to your deployed domain (e.g., https://sigma-labs.com).
# 3. Generate a secure NEXTAUTH_SECRET (e.g., `openssl rand -base64 32`).
# 4. Save this file in your project root (same folder as package.json).
# 5. Restart your dev server after editing this file.